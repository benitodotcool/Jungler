
<div class="form-container">
<div class="title-container">
<h2>Connexion</h2>
</div>
<div>
<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div>
    
    <div class="fieldstyle inset">
    <%= f.email_field :email, placeholder:'email', autofocus: true, autocomplete: "email", class:"login-field" %>
  </div>
  </div>

  <div>
    <%= f.label :password, class: "input-text" %><br />
    <div class="fieldstyle inset">
    <%= f.password_field :password, placeholder:'email' autocomplete: "current-password", class:"login-field" %>
    </div>
  </div>

  <% if devise_mapping.rememberable? %>
    <div class="field">
      <%= f.check_box :remember_me %>
      <%= f.label :remember_me %>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit "Connexion", class: "input-send" %>
  </div>
<% end %>
</div>
<%= render "devise/shared/links" %>
</div>
